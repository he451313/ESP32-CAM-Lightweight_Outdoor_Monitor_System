# ESP32-CAM 低功耗監控系統

這是一個為戶外長時間監測設計的視覺系統專案。

## 主要流程重點

* **雙重影像邏輯**：低解析度比對動態，高解析度彩色存檔。
* **省電邏輯**：利用 Light Sleep 模式，延長電池續航力。

## 使用說明

* `src/main.cpp`：ESP32-CAM 韌體，修改 `main.cpp` 中的 `USE_REAL_SD_CARD` 巨集來切換儲存模式。
* `serial_rx.py`：電腦端接收程式，負責保存 JPEG 照片。
* `.gitignore`：已設定過濾照片與編譯暫存檔。
